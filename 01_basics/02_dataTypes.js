"use strict"; //treat all JS code as newer version

//alert(3 + 3) //We are using nodejs, not browser
//important documents for javascript : tc39, mdm

let Name = "Prakarti"
let age = 18
let isLoggedIn = false
//primitive data types : number, bigint, string, boolean, nuii(it is a standalone value), undefined, symbol(it is used when we ned to find the unique)

//object

console.log(typeof Name);
console.log(typeof null); //object
console.log(typeof undefined); //undefined